<resources>

  <!-- Base application theme. -->
  <style name="AppTheme"
      parent="Theme.AppCompat.Light.DarkActionBar">
    <!-- Customize your theme here. -->
    <item name="colorPrimary">@color/colorPrimary</item>
    <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
    <item name="colorAccent">@color/colorAccent</item>
  </style>

  <style name="AppTheme.NoActionBar">
    <item name="windowActionBar">false</item>
    <item name="windowNoTitle">true</item>
  </style>

  <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>
  <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

  <style name="ActionBarThemeOverlay" parent="AppTheme.AppBarOverlay">
    <item name="android:background">@color/gray_900</item>
  </style>


  <style name="MovieDetailsToolbar" parent="AppTheme.AppBarOverlay">
    <item name="android:background">@null</item>
    <item name="titleTextAppearance">@style/MovieDetailsToolbar.Title</item>
  </style>

  <style name="MovieDetailsToolbar.Title" parent="TextAppearance.Widget.AppCompat.Toolbar.Title">
    <item name="android:textStyle">normal</item>
    <item name="android:textSize">36sp</item>
    <item name="android:gravity">center_vertical</item>

  </style>
</resources>
